'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function AdminLogin(){const[u,setU]=useState('');const[p,setP]=useState('');const[e,setE]=useState('');const[loading,setLoading]=useState(false);const router=useRouter();async function submit(ev:React.FormEvent){ev.preventDefault();setLoading(true);setE('');const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p})});if(r.ok)router.push('/admin');else{const j=await r.json();setE(j.error||'Giriş başarısız')}setLoading(false)}return <form onSubmit={submit} className="w-full max-w-md rounded-[2rem] border border-black bg-yellow p-7 shadow-[8px_8px_0_#161616]"><div className="scribble text-xs">KIFUKI / ADMIN</div><h1 className="mt-3 text-4xl font-bold">Studio control.</h1><div className="mt-8 space-y-3"><input value={u} onChange={e=>setU(e.target.value)} placeholder="Kullanıcı adı" className="w-full rounded-xl border border-black bg-paper p-4 outline-none"/><input value={p} onChange={e=>setP(e.target.value)} type="password" placeholder="Şifre" className="w-full rounded-xl border border-black bg-paper p-4 outline-none"/></div>{e&&<p className="mt-3 text-sm font-bold text-red-700">{e}</p>}<button disabled={loading} className="mt-5 w-full rounded-xl bg-black p-4 font-bold text-white">{loading?'Giriş yapılıyor…':'Giriş yap'}</button></form>}
