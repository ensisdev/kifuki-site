import {NextResponse} from 'next/server';import {randomUUID} from 'node:crypto';import {isAdmin} from '@/lib/auth';import {getArtworks,saveArtwork,deleteArtwork} from '@/lib/db';export async function GET(){return NextResponse.json(await getArtworks())}export async function POST(req:Request){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();const a={id:b.id||randomUUID(),title:b.title,image:b.image,description:b.description||'',year:Number(b.year)||new Date().getFullYear(),featured:Boolean(b.featured)};return NextResponse.json(await saveArtwork(a))}export async function DELETE(req:Request){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});const {id}=await req.json();await deleteArtwork(id);return NextResponse.json({ok:true})}
