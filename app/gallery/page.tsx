import {getArtworks} from '@/lib/db';import Image from 'next/image';import Reveal from '@/components/Reveal';export default async function Gallery(){const arts=await getArtworks();return <main className="min-h-screen px-5 pb-28 pt-32 sm:px-10"><div className="mx-auto max-w-7xl"><span className="scribble text-xs text-black/50">KIFUKI / GALLERY</span><h1 className="mt-5 text-6xl font-bold tracking-tight sm:text-8xl">Gallery<span className="text-violet">.</span></h1><div className="mt-12 columns-2 gap-4 md:columns-3">{arts.map((a,i)=><Reveal key={a.id} delay={(i%3)*.06}><figure className="mb-4 break-inside-avoid"><div className="relative aspect-[4/5] overflow-hidden rounded-3xl border border-black/15"><Image src={a.image} alt={a.title} fill className="object-cover transition duration-700 hover:scale-105" sizes="33vw"/></div><figcaption className="px-2 py-3 text-sm font-bold">{a.title}<span className="float-right text-black/40">{a.year}</span></figcaption></figure></Reveal>)}</div></div></main>}
