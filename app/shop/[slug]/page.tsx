import {getProduct} from '@/lib/db';import {notFound} from 'next/navigation';import Image from 'next/image';import Link from 'next/link';import {ArrowUpRight,ArrowLeft} from 'lucide-react';export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const p=await getProduct((await params).slug);if(!p)notFound();return <main className="min-h-screen px-5 pb-28 pt-32 sm:px-10"><div className="mx-auto max-w-6xl"><Link href="/shop" className="text-xs font-bold uppercase"><ArrowLeft size={14} className="mr-2 inline"/>Back to shop</Link><div className="mt-8 grid gap-10 md:grid-cols-2"><div className="relative aspect-square overflow-hidden rounded-[2rem] border border-black/15 bg-white"><Image src={p.image} alt={p.name} fill className="object-cover"/></div><div className="flex flex-col justify-center"><span className="scribble text-xs text-black/50">{p.category}</span><h1 className="mt-4 text-5xl font-bold tracking-tight sm:text-7xl">{p.name}</h1><p className="mt-6 max-w-lg leading-7 text-black/60">{p.description}</p><div className="mt-8 text-2xl font-bold">{p.price} {p.currency}</div><a href={p.shopierUrl||'https://www.shopier.com/kifuki'} target="_blank" rel="noreferrer" className="mt-8 inline-flex w-fit rounded-full bg-black px-7 py-4 text-sm font-bold text-white">Shopier'den satın al <ArrowUpRight size={17} className="ml-2"/></a><p className="mt-4 text-xs text-black/45">Ödeme ve sipariş süreci Shopier üzerinde gerçekleşir.</p></div></div></div></main>}
