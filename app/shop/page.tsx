import {getProducts} from '@/lib/db';import ProductCard from '@/components/ProductCard';import Localized from '@/components/Localized';export default async function Shop(){const ps=await getProducts();const cats=['All',...Array.from(new Set(ps.map(p=>p.category)))];return <main className="min-h-screen px-5 pb-28 pt-32 sm:px-10"><div className="mx-auto max-w-7xl"><span className="scribble text-xs text-black/50">KIFUKI / SHOP</span><h1 className="mt-5 text-6xl font-bold tracking-tight sm:text-8xl"><Localized path="nav.shop"/></h1><p className="mt-5 max-w-xl text-black/60">Küçük şeyler, büyük enerji. Satın alma işlemi Shopier üzerinden tamamlanır.</p><div className="mt-10 flex flex-wrap gap-2">{cats.map(c=><span key={c} className="rounded-full border border-black/20 px-4 py-2 text-xs font-bold">{c}</span>)}</div><div className="mt-12 grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">{ps.map(p=><ProductCard key={p.id} p={p}/>)}</div></div></main>}
