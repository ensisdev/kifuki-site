'use client';import {useEffect,useState} from 'react';import type {Lang} from '@/lib/i18n';
export default function LanguageToggle(){const[l,setL]=useState<Lang>('tr');useEffect(()=>{const x=localStorage.getItem('kifuki-lang') as Lang|null;if(x)setL(x)},[]);function set(x:Lang){setL(x);localStorage.setItem('kifuki-lang',x);window.dispatchEvent(new CustomEvent('kifuki-lang',{detail:x}))}return <div className="flex items-center gap-1 rounded-full border border-black/15 bg-white/40 p-1 text-[11px] font-bold"><button onClick={()=>set('tr')} className={`rounded-full px-2 py-1 ${l==='tr'?'bg-black text-white':''}`}>TR</button><button onClick={()=>set('en')} className={`rounded-full px-2 py-1 ${l==='en'?'bg-black text-white':''}`}>EN</button></div>}
