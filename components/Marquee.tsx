'use client';import {motion} from 'framer-motion';export default function Marquee(){return <div className="overflow-hidden border-y border-black py-3"><motion.div className="flex w-max gap-8 whitespace-nowrap text-xs font-bold uppercase tracking-[.3em]" animate={{x:['0%','-50%']}} transition={{repeat:Infinity,duration:22,ease:'linear'}}>{Array.from({length:12}).map((_,i)=><span key={i}>STICKERS ✦ ART STUDIO ✦ KIFUKI ✦ DRAW SOMETHING NICE ✦</span>)}</motion.div></div>}
