'use client';import Image from 'next/image';import Link from 'next/link';import StickerTag from './StickerTag';import type {Product} from '@/lib/types';import {ArrowUpRight} from 'lucide-react';export default function ProductCard({p}:{p:Product}){return <article className="group relative"><Link href={`/shop/${p.slug}`} className="block"><div className="relative aspect-[4/5] overflow-hidden rounded-[2rem] border border-black/15 bg-white"><Image src={p.image} alt={p.name} fill className="object-cover transition duration-700 group-hover:scale-105" sizes="(max-width:768px) 50vw, 25vw"/><div className="absolute left-4 top-4 rounded-full border border-black bg-paper px-3 py-1 text-[10px] font-bold uppercase">{p.category}</div><StickerTag href={p.shopierUrl}/></div><div className="mt-3 flex items-start justify-between gap-3"><div><h3 className="font-bold">{p.name}</h3><p className="mt-1 text-xs text-black/55">{p.description}</p></div><span className="whitespace-nowrap text-sm font-bold">{p.price} {p.currency}</span></div></Link></article>}
